version: '3.8'

services:
  vaultshield:
    build: .
    container_name: vaultshield-privacy-studio
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
