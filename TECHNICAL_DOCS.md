# Technical Documentation | مستندات فنی پلتفرم VaultShield

## ۱. معماری سیستم و لایه‌بندی (System Architecture)

پروژه **VaultShield** یک مجموعه ماژولار برای ارتقای امنیت اطلاعات، امحای ردهای دیجیتال و انتقال امن محتوا در کانال‌های عمومی است:

```
┌─────────────────────────────────────────────────────────────┐
│                 رابط‌های کاربری و دسترسی                     │
│  [PyQt6 Desktop GUI]  /  [Web Studio SPA]  /  [CLI Runner]  │
└──────────────────────────────┬──────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────┐
│                  هسته پردازشی (Core Engine)                  │
│  ├── Metadata Wiper (Raw Canvas & PDF Purge)                │
│  ├── Steganography Vault (AES-256-GCM & Bitstream LSB)      │
│  ├── File Shredder (DoD 5220.22-M Multi-Pass Overwrite)     │
│  ├── Format Validator & Capacity Analyzer                   │
│  └── i18n Engine (Dynamic RTL / LTR Persian & English)      │
└──────────────────────────────┬──────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────┐
│                    DevOps و بسته‌بندی                       │
│     [GitHub Actions CI/CD]  /  [Docker]  /  [PyInstaller]   │
└─────────────────────────────────────────────────────────────┘
```

---

## ۲. ساختار جامع فایل‌ها و پوشه‌بندی (Repository Structure)

```text
A:\AGENT\github\VaultShield\
├── .github\
│   └── workflows\
│       └── build-release.yml          # ساخت خودکار فایل exe و انتشار Releases
├── .gitignore                         # قوانین نادیده‌گیری فایل‌های موقت
├── Dockerfile                         # ایمیج داکر برای اجرای وب‌استودیو
├── docker-compose.yml                 # ارکستراسیون آسان کانتینر
├── LICENSE                            # مجوز رسمی MIT
├── README.md                          # معرفی کامل و ویترین گیت‌هاب (دوزبانه)
├── TECHNICAL_DOCS.md                  # مستندات فنی حاضر
├── BRAIN.md                           # چشم‌انداز محصول و نیازمندی‌های بازار
├── requirements.txt                   # پیش‌نیازهای پایتون
├── main.py                            # نقطه ورود یکپارچه CLI / GUI / Web
├── run_gui.bat                        # اجرای سریع GUI در ویندوز
├── run_web.bat                        # اجرای سریع Web Server در ویندوز
├── build_exe.bat                      # کامپایلر محلی با PyInstaller
├── VaultShield.spec                   # کانفیگ مستقل PyInstaller (نسخه ۶+)
├── src\
│   ├── core\
│   │   ├── metadata_wiper.py          # پاک‌سازی متادیتا، موقعیت GPS و هدرهای EXIF
│   │   ├── steganography.py           # موتور رمزنگاری AES-256-GCM و تزریق LSB
│   │   ├── shredder.py                # امحای چندمرحله‌ای فایل طبق DoD 5220.22-M
│   │   ├── validator.py               # اعتبارسنجی فایل‌ها و ظرفیت تصاویر حامل
│   │   └── i18n.py                    # موتور ترجمه دوزبانه (فارسی RTL و انگلیسی LTR)
│   ├── gui\
│   │   ├── main_window.py             # پنجره اصلی PyQt6 با انیمیشن و لاگ
│   │   ├── widgets.py                 # دراپ‌زون امن و کارت‌های فایل
│   │   ├── icons.py                   # مدیریت کش برداری آیکون‌های SVG
│   │   └── styles.py                  # استایل‌های دارک نئونی و قوانین RTL
│   ├── web\
│   │   ├── server.py                  # سرور HTTP استاندارد با اندپوینت‌های REST
│   │   └── static\
│   │       ├── index.html             # رابط کاربری وب تک‌صفحه‌ای (SPA)
│   │       ├── style.css              # استایل ریسپانسیو با رعایت جهت LTR باکس‌ها
│   │       └── app.js                 # منطق کلاینت و ترجمه آنی دوطرفه
│   └── cli\
│       └── cli_runner.py              # واسط خط فرمان حرفه‌ای
└── tests\
    ├── test_metadata_wiper.py         # تست پاک‌سازی متادیتا
    ├── test_steganography.py          # تست رمزنگاری AES-256 و استخراج LSB
    ├── test_shredder.py               # تست امحای امن فایل و پاک‌سازی بایت‌ها
    └── test_validator.py              # تست اعتبارسنجی فرمت‌ها و ظرفیت LSB
```

---

## ۳. شرح فنی ماژول‌ها

### ۳.۱. پاک‌سازی متادیتا (`src/core/metadata_wiper.py`)
- استفاده از تکنیک Raw Canvas Reconstruction: پیکسل‌های تصویر استخراج شده و روی یک بوم (Canvas) کاملاً نو ترسیم می‌شوند تا هیچ‌گونه متادیتای باقیمانده، بندانگشتی (Thumbnail) مخفی یا استریم اختصاصی دوربین ذخیره نشود.
- در اسناد PDF، دیکشنری Document Info (`/Author`, `/Title`, `/CreationDate`, `/ModDate`) و استریم XMP به طور کامل پالایش می‌شوند.

### ۳.۲. پنهان‌نگاری پیشرفته (`src/core/steganography.py`)
- ساختار بسته رمزگذاری‌شده:
  `[۴ بایت Magic: VLT1] + [۱۶ بایت Salt] + [۱۲ بایت Nonce] + [۴ بایت طول داده] + [رمزنگاری‌شده AES-256-GCM همراه با ۱۶ بایت Auth Tag]`
- مشتق‌سازی کلید با الگوریتم **PBKDF2-HMAC-SHA256** با ۱۰۰,۰۰۰ دور محاسباتی.
- تزریق نامرئی بیت‌ها به کانال‌های رنگی بایت‌های خام (`tobytes()`) با کمترین تغییر در سطح بیت (LSB) بدون هیچ افت کیفیتی در تصویر.

### ۳.۳. امحای فیزیکی فایل (`src/core/shredder.py`)
- استاندارد DoD 5220.22-M:
  - دور ۱: رونویسی با بایت‌های صفر (`0x00`)
  - دور ۲: رونویسی با بایت‌های یک (`0xFF`)
  - دور ۳: رونویسی با بایت‌های تصادفی رمزنگاری‌شده (`os.urandom`)
- فراخوانی مستقیم `os.fsync` برای دور زدن کش سیستم‌عامل و نوشتن بایت‌ها روی سلول‌های حافظه فیزیکی.
- تغییر نام فایل به کاراکترهای تصادفی، کوتاه کردن طول به صفر و در نهایت حذف فیزیکی.

---

## ۴. راستی‌آزمایی و نتایج تست‌ها

اجرای تست‌های واحد خودکار با دستور:
```bash
cmd /c python -m unittest discover tests
```
- **۶ تست خودکار واحد** شامل اعتبارسنجی فرمت‌ها، ظرفیت تزریق، پاک‌سازی EXIF، رمزنگاری AES-256-GCM، اعتبارسنجی خطای رمز عبور اشتباه و امحای فیزیکی فایل با موفقیت کامل (**OK**) پاس شدند.
